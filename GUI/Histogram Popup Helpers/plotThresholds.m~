function lineHandles = plotThresholds(axes, thresholds, lineHandles)
    yLims = get(axes, 'YLim');
    
    yMin = yLims(1);
    yMax = yLims(2);
    
    for i=1:length(lineHandles)
        delete(lineHandles{i});
    end
    
    numThresholds = length(thresholds);
    
    lineHandles = cell(numThresholds
    
    for i=1:numThresholds
        
    end
    
    x = [muscleLowerThreshold, muscleLowerThreshold];
    y = [yMin, yMax];
    
    hold on;
    lineHandle = plot(x, y, 'r');
    hold off;
end

